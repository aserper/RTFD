{
  "mcpServers": {
    "rtfd": {
      "command": "python",
      "args": ["-m", "RTFD.server"],
      "env": {
        "RTFD_FETCH": "${RTFD_FETCH:true}",
        "VERIFIED_BY_PYPI": "${VERIFIED_BY_PYPI:false}",
        "GITHUB_AUTH": "${GITHUB_AUTH:false}",
        "GITHUB_TOKEN": "${GITHUB_TOKEN:}"
      }
    }
  }
}
